{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/docker-testapp-main/package.json",
                "localRoot": "${workspaceFolder}/docker-testapp-main"
            }
        }
    ]
}